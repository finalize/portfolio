---
date: 2026-09-08
status: reported
summary: コード面は異常なし。PR自動マージ滞留の再発をissueに追記
pr: 41
issue: 21
---

## 点検結果（すべて問題なし）

- `pnpm audit --prod` / `pnpm audit`: 脆弱性0件。
- `pnpm outdated`: メジャー更新は `typescript`（6.0.3 → 7.0.2）のみ。既存issue #3で追跡中のため変更なし。
  `wrangler`（minor）はDependabotの対象。
- `pnpm build`: 警告なく成功（33ページ生成）。
- リンク: `dist/` 内の内部リンクはすべて実在。外部リンクもすべて200（x.com・npmjs.comの403のみ対象外）。
- `astro.config.mjs` の `site` と `public/robots.txt` の Sitemap 行は一致。
- `pnpm run check:contrast`（`pnpm check` にも含む）: すべての組み合わせが基準を満たす。
- `pnpm check`: 0エラー・0警告・1ヒント（`TermPic.astro` の `Props` 未使用ヒントは既知の誤検知、変更なし）。
- alt属性・見出し階層・meta descriptionに欠落なし。

## 直したこと

なし（直すべき問題が見つからなかったため、コードの変更はなし）。

## 直さずに残したこと

- `typescript` のメジャー更新（issue #3で追跡中、変更なし）。
- 日次PRの自動マージ滞留（issue #21で追跡中）が2026-09-06分（PR #38）・2026-09-07分（PR #40）で2件連続して再発し、いずれも `OPEN` のまま止まっていることを確認した。2026-09-01に見つかった回避策（PR番号を事前予測し `pr:` を書いた状態で1回だけpush）が09-06・09-07では適用されず2回pushしていたため再発したとみられる。今回はこの回避策を使って1回のpushで完結させた。issue #21にコメントで状況を追記した。
