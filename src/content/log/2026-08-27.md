---
date: 2026-08-27
status: reported
summary: PR #19 が自動マージされず止まっていたため issue 化
pr: 22
issue: 21
---

## 点検結果

- `pnpm audit --prod` / `pnpm audit`: 脆弱性なし
- `pnpm outdated`: メジャー更新は `typescript` 6.0.3 → 7.0.2 のみ（既存 issue #3 で追跡中、変更なし）。`astro`（patch）・`wrangler`（minor）・`sharp`（patch）は Dependabot の対象
- `pnpm build`: 警告なく成功（17ページ生成）
- 内部リンクはすべて `dist/` 内に実在。外部リンクもすべて 200（npmjs.com の 403 のみ対象外）
- `astro.config.mjs` の `site` と `public/robots.txt` の Sitemap 行は一致
- `pnpm run check:contrast`: すべての組み合わせが基準を満たす
- alt 属性 / 見出し階層 / meta description: 欠落なし
- `pnpm check`: 0 エラー・0 警告・1 ヒント（`TermPic.astro` の `Props` 未使用ヒントは 2026-08-25 の点検で誤検知と確認済み、変更なし）

## 直さず issue にしたこと

前日（2026-08-26）分の日次点検 PR #19 が、GitHub Actions 側の `action_required`（要承認）により自動マージされずに止まっていた。
`push` イベント側の CI は成功していたが、`pull_request` イベント側の CI / Claude Review / Dependabot auto-merge が
いずれも承認待ちのままで、ブランチ保護の必須チェックが満たされていなかった。空コミットで再トリガーを試みたが再発。
`gh api` での承認・再実行はいずれも権限不足（403）で実行できず、コードの変更では直せないためissue #21 を作成した。

直すものは見つからなかったため、コードの変更はなし。
