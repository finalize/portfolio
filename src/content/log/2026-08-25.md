---
date: 2026-08-25
status: ok
summary: 点検のみ、異常なし
---

## 点検結果

- `pnpm audit --prod` / `pnpm audit`: 脆弱性なし
- `pnpm outdated`: `typescript` 6.0.3 → 7.0.2（メジャー、issue #3 で追跡中）、`astro` 7.2.4 → 7.2.6・`termpic` 0.2.1 → 0.2.2（いずれも patch、Dependabot の自動化対象）
- `pnpm build`: 警告なく成功（16ページ生成）
- 内部リンク（25件）はすべて `dist/` 内に実在。外部リンク（12件）もすべて 200（npmjs.com のみ 403 だが対象外扱い）
- `astro.config.mjs` の `site` と `public/robots.txt` の Sitemap 行は一致
- `pnpm run check:contrast`: すべての組み合わせが基準を満たす
- alt 属性 / 見出し階層 / meta description: 欠落なし
- `pnpm check`（`astro check`）は 0 エラー・0 警告・1 ヒント。`TermPic.astro` の `Props` インターフェースが「未使用」と出るが、実際は `Astro.props` の分割代入で全プロパティを使用しており誤検知と見られる（トップレベル `await` を持つのはこのコンポーネントのみで、他の `interface Props` を使うファイルでは出ない）。ビルド・CI には影響しないため今回は変更せず

直すものは見つからなかったため、コードの変更はなし。TypeScript のメジャー更新は既存の issue #3 で追跡中のため、新規 issue は作成していない。
