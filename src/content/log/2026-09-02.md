---
date: 2026-09-02
status: ok
summary: 日次点検、異常なし
pr: 34
issue: 21
---

日次の自動点検を実施した。直すべき問題は見つからなかったため、コードの変更は行っていない。

- `pnpm audit --prod` / `pnpm audit`: 脆弱性なし
- `pnpm outdated`: `@astrojs/sitemap` 3.7.3 → 3.7.4・`astro` 7.2.9 → 7.2.10・`wrangler` 4.127.1 → 4.128.0（いずれも minor / patch で Dependabot の対象）。`typescript` 6.0.3 → 7.0.2 のメジャー更新は既存の issue #3 で追跡中のため、新規 issue は作成していない
- `pnpm check`（`astro check` 相当）: 0 エラー・0 警告・1 ヒント（`TermPic.astro` の `Props` 未使用ヒントは既知の誤検知）
- `pnpm run check:contrast`: dark / light とも本文・リンク等のテキストの組み合わせは基準を満たす
- `pnpm build`: 警告なく成功（30ページ生成）
- `astro.config.mjs` の `site`（`https://shogo-site.shgysd.workers.dev`）と `public/robots.txt` の Sitemap 行は一致
- `dist/` 内の内部リンクはすべて実在するファイルを指している
- 外部リンク25件はすべて 200（npmjs.com の2件のみ 403 で対象外、既知の bot ブロック）
- 画像の alt 抜け・見出し階層の飛び・meta description の欠落は無し

issue #21（PR 2回目以降の push で `action_required` になり自動マージが止まる問題）の回避策に従い、
このログも次番号（34）を予測して `pr:` に書いた状態で1回の push にまとめている。
