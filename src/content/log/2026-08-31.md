---
date: 2026-08-31
status: ok
summary: 日次点検、異常なし
pr: 30
issue: 21
---

日次の自動点検を実施した。直すべき問題は見つからなかったため、コードの変更は行っていない。

- `pnpm audit --prod` / `pnpm audit`: 脆弱性なし
- `pnpm outdated`: `astro` 7.2.6 → 7.2.9・`wrangler` 4.125.0 → 4.127.1・`sharp` 0.35.3 → 0.35.4（いずれも minor / patch で Dependabot の対象）。`typescript` 6.0.3 → 7.0.2 のメジャー更新は既存の issue #3 で追跡中のため、新規 issue は作成していない
- `pnpm check`: 0 エラー・0 警告・1 ヒント（`TermPic.astro` の `Props` 未使用ヒントは既知の誤検知）
- `pnpm run check:contrast`: dark / light とも本文・リンク等のテキストの組み合わせは基準を満たす
- `pnpm build`: 警告なく成功（28ページ生成）
- `astro.config.mjs` の `site`（`https://shogo-site.shgysd.workers.dev`）と `public/robots.txt` の Sitemap 行は一致
- `dist/` 内の内部リンクはすべて実在するファイルを指している
- 外部リンク18件はすべて 200
- 画像の alt 抜け・見出し階層の飛び・meta description の欠落は無し

## 追記

この PR 自身も、2回目の push（PR 番号追記）で `action_required` が発生し `mergeStateStatus: BLOCKED` になった。issue #21 で継続追跡している既知の現象と同一のため、新規 issue は作らず issue #21 にコメントを追記した。承認は人による対応が必要。
