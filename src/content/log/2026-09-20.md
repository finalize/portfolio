---
date: 2026-09-20
status: ok
summary: "点検のみ、異常なし"
pr: 56
issue: 21
---

## 点検結果

- `pnpm audit --prod` / `pnpm audit`: 脆弱性なし
- `pnpm outdated`: メジャー更新は `typescript`（6.0.3 → 7.0.2）のみで、既存 issue #3 で追跡中。`@astrojs/cloudflare`・`astro`・`@anthropic-ai/sdk`・`wrangler` は minor/patch で Dependabot の対象
- `pnpm build`: 警告なく成功（44ページ生成）
- `dist/` から内部リンク48件・外部リンク84件を抽出し、内部リンクはすべて実在、外部リンクはすべて200（npmjs.com の403のみ既知のbotブロックで対象外）を確認
- `astro.config.mjs` の `site` と `public/robots.txt` の Sitemap 行は一致
- `pnpm run check:contrast`（`pnpm check` にも含む）: すべての組み合わせが基準を満たす
- 画像（`<img>`）のalt欠落なし。見出し階層の飛びなし。meta descriptionの欠落なし
- `pnpm check`: 0 エラー・0 警告・1 ヒント（`TermPic.astro` の `Props` 未使用ヒントは既知の誤検知、変更なし）

## 直さなかったこと

コード面で直すべき問題は見つからなかったため、変更なし。

## 継続中の問題

- `typescript` のメジャー更新（issue #3 で追跡中、変更なし）。
- issue #21 で報告している PR #51（2026-09-16分）の自動マージ滞留は今回も `OPEN` のまま（開いてから4日経過）。追加の対処（承認・マージ）は権限がなく引き続き実施できない。今回のPRは既知の回避策（次のPR番号を事前予測し `pr:` フロントマターに書いた状態で1回だけpush）を用いて作成する。
