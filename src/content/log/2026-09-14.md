---
date: 2026-09-14
status: ok
summary: 点検のみ、異常なし
pr: 48
issue: 21
---

## 点検結果

- `pnpm audit --prod` / `pnpm audit`: 脆弱性なし
- `pnpm outdated`: メジャー更新は `typescript` 6.0.3 → 7.0.2 のみ（既存 issue #3 で追跡中、変更なし）。`@anthropic-ai/sdk`（patch）・`wrangler`（minor）は Dependabot の対象
- `pnpm build`: 警告なく成功（36ページ生成）
- 内部リンクはすべて `dist/` 内に実在。外部リンクもすべて200（npmjs.com・x.comの403のみ対象外）
- `astro.config.mjs` の `site` と `public/robots.txt` の Sitemap 行は一致
- `pnpm run check:contrast`（`pnpm check` にも含む）: すべての組み合わせが基準を満たす
- 画像（`<img>`）は存在せず、alt欠落なし。見出し階層の飛びなし。meta descriptionの欠落なし
- `pnpm check`: 0 エラー・0 警告・1 ヒント（`TermPic.astro` の `Props` 未使用ヒントは既知の誤検知、変更なし）

## 直さなかったこと

コード面で直すべき問題は見つからなかったため、変更なし。

## 継続中の問題

issue #21 で報告している自動マージ滞留について、これまで残っていた PR #38・#40 が 2026-09-13 に `MERGED` になっていることを確認した（人手対応とみられる）。現時点で滞留している PR はない。根本原因（Actions の承認設定）が解消されたかは確認できないため、issue は引き続きオープンのまま経過を見る。今回のログPRも既知の回避策（PR番号を事前予測し、1回のpushで完結させる）を用いて作成した。
